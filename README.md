# ucomQuest_TelegramBot

## Installation

```bash
$ npm install
```
###  replace `example.env` file to `.env`

## Running the app

```bash
# starting
$ npm start
```
## Login `Name` and `password` in `.env` file
